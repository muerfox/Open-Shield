# All of these (plus their transitive deps) are vendored as wheels under
# ./vendor/wheels, so `docker build` installs with --no-index --find-links
# and never hits PyPI. See vendor/wheels/VERSIONS.md.
fastapi==0.115.6
uvicorn==0.32.1
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
redis==5.2.1
httpx==0.28.1
jinja2==3.1.4
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
itsdangerous==2.2.0
python-multipart==0.0.19
